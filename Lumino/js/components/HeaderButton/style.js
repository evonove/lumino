import { StyleSheet } from 'react-native';


const styles = StyleSheet.create({
  buttonText: {
    fontSize: 17,
    color: 'white',
    fontWeight: 'bold',
    margin: 10,
  }
});


export default styles;
